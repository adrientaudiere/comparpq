% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_lpq.R
\name{adonis_lpq}
\alias{adonis_lpq}
\title{PERMANOVA analysis on each phyloseq object in a list_phyloseq}
\usage{
adonis_lpq(
  x,
  formula,
  dist_method = "bray",
  na_remove = FALSE,
  correction_for_sample_size = FALSE,
  rarefy_nb_seqs = FALSE,
  by = "margin",
  verbose = TRUE,
  ...
)
}
\arguments{
\item{x}{(required) A list_phyloseq object.}

\item{formula}{(character, required) The right part of a formula for
\code{\link[vegan:adonis]{vegan::adonis2()}}. Variables must be present in the \code{sample_data} slot
of \strong{all} phyloseq objects. The formula should contain variables that
are in the shared modalities.}

\item{dist_method}{(character, default "bray") The distance method to use.
See \code{\link[phyloseq:distance]{phyloseq::distance()}} for available methods.}

\item{na_remove}{(logical, default FALSE) If TRUE, samples with NA values
in the formula variables are removed before analysis.}

\item{correction_for_sample_size}{(logical, default FALSE) If TRUE, adds
library size to the formula following Weiss et al. 2017 recommendations.}

\item{rarefy_nb_seqs}{(logical, default FALSE) If TRUE, rarefy each sample
before computing distances.}

\item{by}{(character, default "margin") The \code{by} argument passed to
\code{\link[vegan:adonis]{vegan::adonis2()}}. Options are "terms", "margin", or NULL.}

\item{verbose}{(logical, default TRUE) If TRUE, print progress messages.}

\item{...}{Additional arguments passed to \code{\link[MiscMetabar:adonis_pq]{MiscMetabar::adonis_pq()}}.}
}
\value{
A tibble with one row per phyloseq object and the following columns:
\describe{
\item{name}{Name of the phyloseq object}
\item{term}{The term(s) from the formula (one row per term if multiple)}
\item{Df}{Degrees of freedom}
\item{SumOfSqs}{Sum of squares}
\item{R2}{R-squared value}
\item{F}{F statistic}
\item{Pr(>F)}{P-value}
\item{partial_R2}{Partial R-squared (if available)}
}
}
\description{
Performs a PERMANOVA analysis using \code{\link[MiscMetabar:adonis_pq]{MiscMetabar::adonis_pq()}} on each
phyloseq object in a list_phyloseq and returns a summary table with the
results.
}
\details{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental"></a>
}}


This function requires that the list_phyloseq type is NOT \code{SEPARATE_ANALYSIS},
as the formula must contain variables that are common across all phyloseq
objects.

The function is a wrapper around \code{\link[MiscMetabar:adonis_pq]{MiscMetabar::adonis_pq()}}, which itself
wraps \code{\link[vegan:adonis]{vegan::adonis2()}}.
}
\examples{
\dontrun{
# Create a list_phyloseq with ROBUSTNESS comparison
lpq <- list_phyloseq(
  list(
    fungi = data_fungi_woNA4height,
    fungi_clust = postcluster_pq(data_fungi_woNA4height)
  ),
  same_bioinfo_pipeline = FALSE
)

# Run PERMANOVA on each phyloseq object
results <- adonis_lpq(lpq, formula = "Height+Time", na_remove = TRUE)
results

# With Jaccard distance
results_jaccard <- adonis_lpq(lpq, formula = "Height", dist_method = "jaccard")
}

}
\seealso{
\code{\link[MiscMetabar:adonis_pq]{MiscMetabar::adonis_pq()}}, \code{\link[vegan:adonis]{vegan::adonis2()}}
}
