% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/refseq_comp_lpq.R
\name{refseq_comp_lpq}
\alias{refseq_comp_lpq}
\title{Compare refseq sequences across all objects in a list_phyloseq}
\usage{
refseq_comp_lpq(x, thresholds = c(0, 1, 2, 3, 5, 10), k = 7, verbose = TRUE)
}
\arguments{
\item{x}{(required) A \link{list_phyloseq} object. All phyloseq objects must have
a populated \verb{@refseq} slot. Supports 2–7 objects (Venn diagrams are
suppressed for > 7).}

\item{thresholds}{(numeric vector, default \code{c(0, 1, 2, 3, 5, 10)}) Maximum
dissimilarity thresholds in percent. \code{0} means exact string matches only;
\code{10} means sequences within 10\% divergence are grouped together.}

\item{k}{(integer, default \code{7}) K-mer size for building presence/absence
profiles. Larger \code{k} increases specificity; smaller \code{k} increases
sensitivity for divergent sequences.}

\item{verbose}{(logical, default \code{TRUE}) If \code{TRUE}, print progress messages.}
}
\value{
A list of class \code{"refseq_comp_lpq_result"} with:
\describe{
\item{\code{venn_plots}}{Named list of ggVennDiagram objects (one per
threshold, names follow \code{"<threshold>_pct"}), or \code{NULL} if
\pkg{ggVennDiagram} is not installed or N > 7.}
\item{\code{results}}{List of per-threshold results, each containing
\code{threshold}, \code{venn_data} (named list of component IDs per set),
and \code{n_shared} (named list of shared cluster counts per pair).}
\item{\code{labels}}{Character vector of phyloseq object names.}
\item{\code{thresholds}}{The thresholds used.}
\item{\code{k}}{The k-mer size used.}
}
}
\description{
For each pair of phyloseq objects in \code{x}, computes pairwise sequence
similarity between \verb{@refseq} slots using k-mer Jaccard similarity, then
groups near-identical sequences across all objects via connected components
(union-find). Returns a list of results per threshold, and one Venn
diagram per threshold (requires \pkg{ggVennDiagram}).

Similarity is approximated as \verb{p ≈ (2j / (1 + j))^(1/k)} where \code{j} is the
Jaccard index of shared k-mer presence and \code{k} is the k-mer size. The
approximation is accurate within ~2 percentage points for identities in the
90–100\% range.
}
\details{
Compare refseq sequences across all objects in a list_phyloseq.\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange"
  alt="lifecycle-experimental"></a>
}}
}
\examples{
lpq <- list_phyloseq(list(
  a = data_fungi_mini,
  b = prune_taxa(taxa_names(data_fungi_mini)[1:20], data_fungi_mini)
))
res <- refseq_comp_lpq(lpq)
}
\author{
Adrien Taudière
}
