% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_refseq.R
\name{compare_refseq_pq}
\alias{compare_refseq_pq}
\title{Compare refseq slots between two phyloseq objects}
\usage{
compare_refseq_pq(
  physeq1,
  physeq2 = NULL,
  name1 = NULL,
  name2 = NULL,
  k = 5,
  max_seqs = 500,
  seed = NULL,
  verbose = TRUE
)
}
\arguments{
\item{physeq1}{(phyloseq or list_phyloseq, required) First phyloseq
object, which must have a \code{refseq} slot. Alternatively, a
\link{list_phyloseq} object; in that case the first two phyloseq objects
are used (with their names) and \code{physeq2} is ignored.}

\item{physeq2}{(phyloseq, default NULL) Second phyloseq object. Must
have a \code{refseq} slot. Ignored when \code{physeq1} is a \link{list_phyloseq}.}

\item{name1}{(character, default NULL) Label for the first phyloseq
object in the output. If NULL, inferred from the \link{list_phyloseq}
names or defaults to \code{"physeq1"}.}

\item{name2}{(character, default NULL) Label for the second phyloseq
object in the output. If NULL, inferred from the \link{list_phyloseq}
names or defaults to \code{"physeq2"}.}

\item{k}{(integer, default 5) k-mer size for nearest-neighbor distance
computation on unique sequences.}

\item{max_seqs}{(integer, default 500) Maximum number of unique sequences
per object to use for distance computation. If there are more, a random
sample is drawn. Set to \code{Inf} to use all (may be slow).}

\item{seed}{(integer, default NULL) Random seed for reproducible sampling
when \code{max_seqs} is exceeded.}

\item{verbose}{(logical, default TRUE) If TRUE, print a summary of the
comparison.}
}
\value{
A list of class \code{"compare_refseq"} with components:
\describe{
\item{\code{n_taxa}}{Named integer vector with the number of taxa in each
object.}
\item{\code{shared_names}}{Character vector of taxa names present in both
objects.}
\item{\code{unique_names_1}}{Taxa names only in \code{physeq1}.}
\item{\code{unique_names_2}}{Taxa names only in \code{physeq2}.}
\item{\code{shared_seqs}}{Character vector of unique DNA sequences found in
both objects (regardless of taxa name).}
\item{\code{unique_seqs_1}}{DNA sequences only in \code{physeq1}.}
\item{\code{unique_seqs_2}}{DNA sequences only in \code{physeq2}.}
\item{\code{same_name_diff_seq}}{A data frame of taxa with the same name but
different sequences across objects. Columns: \code{taxa_name},
\code{seq_physeq1}, \code{seq_physeq2}.}
\item{\code{same_seq_diff_name}}{A data frame of sequences shared between
objects under different names. Columns: \code{sequence}, \code{name_physeq1},
\code{name_physeq2}.}
\item{\code{mean_nn_kmer_dist_1}}{Mean nearest-neighbor k-mer distance from
sequences unique to \code{physeq1} to the closest sequence in \code{physeq2}.
\code{NA} if no unique sequences.}
\item{\code{mean_nn_kmer_dist_2}}{Mean nearest-neighbor k-mer distance from
sequences unique to \code{physeq2} to the closest sequence in \code{physeq1}.
\code{NA} if no unique sequences.}
}
}
\description{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange"
  alt="lifecycle-experimental"></a>
}}


Performs a detailed comparison of the reference sequences (\code{refseq} slot)
between two phyloseq objects. Identifies shared and unique ASVs/OTUs both
by taxa name and by actual DNA sequence content.

This is useful to detect subtle differences between two phyloseq objects
that may share the same samples but differ in their ASV/OTU composition,
e.g. after different bioinformatics pipelines or filtering steps.

When unique (non-shared) sequences exist, the mean nearest-neighbor k-mer
distance from each unique sequence to the other set is computed. This
gives a sense of how different the unmatched sequences are from their
closest counterpart in the other object.
}
\examples{
# Compare a phyloseq object with itself (all shared)
res <- compare_refseq_pq(data_fungi_mini, data_fungi_mini)
res

# Compare with a subset
sub <- prune_taxa(taxa_names(data_fungi_mini)[1:20], data_fungi_mini)
res2 <- compare_refseq_pq(data_fungi_mini, sub,
  name1 = "full", name2 = "subset"
)
res2

# From a list_phyloseq (uses the first two objects and their names)
lpq <- list_phyloseq(list(full = data_fungi_mini, subset = sub))
res3 <- compare_refseq_pq(lpq)
res3
}
\author{
Adrien Taudiere
}
