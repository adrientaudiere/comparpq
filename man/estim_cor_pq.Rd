% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estim_pq.R
\name{estim_cor_pq}
\alias{estim_cor_pq}
\title{Estimation statistics for numeric variable correlation on a phyloseq
object}
\usage{
estim_cor_pq(
  physeq,
  variable,
  q = c(0, 1, 2),
  custom_fn = NULL,
  method = "pearson",
  resamples = 5000,
  ci = 95,
  na_remove = TRUE
)
}
\arguments{
\item{physeq}{(phyloseq, required) A phyloseq object.}

\item{variable}{(character, required) The name of a numeric column in
\code{sample_data}.}

\item{q}{(numeric vector, default \code{c(0, 1, 2)}) The q
values for Hill number computation.}

\item{custom_fn}{(function, default NULL) An optional custom diversity
function (see \code{\link[=estim_diff_pq]{estim_diff_pq()}} for details).}

\item{method}{(character, default \code{"pearson"}) Correlation method.
One of \code{"pearson"}, \code{"spearman"}, \code{"kendall"}.}

\item{resamples}{(integer, default 5000) Number of bootstrap
resamples.}

\item{ci}{(numeric, default 95) Confidence interval level (0-100).}

\item{na_remove}{(logical, default TRUE) If TRUE, samples with NA in
\code{variable} are removed.}
}
\value{
A list of class \code{"estim_cor_pq_result"} with components:
\describe{
\item{data}{The diversity data.frame used for analysis}
\item{correlations}{A tibble with columns: \code{metric}, \code{estimate},
\code{ci_lower}, \code{ci_upper}, \code{method}, \code{pvalue}}
\item{regressions}{A tibble with columns: \code{metric}, \code{intercept},
\code{slope}, \code{slope_ci_lower}, \code{slope_ci_upper}}
\item{plots}{A named list of ggplot2 scatter plots with
regression line and bootstrap CI ribbon (one per metric)}
}
}
\description{
Computes diversity metrics (Hill numbers by default) per sample and
assesses their relationship with a numeric variable using bootstrap
confidence intervals for correlation coefficients and regression
slopes.
}
\details{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange"
alt="lifecycle-experimental"></a>
}}
}
\examples{
\dontrun{
library(phyloseq)
data("data_fungi", package = "MiscMetabar")

# Add a numeric variable for demonstration
sam <- sample_data(data_fungi)
sam$lib_size <- sample_sums(data_fungi)
sample_data(data_fungi) <- sam

res <- estim_cor_pq(data_fungi, variable = "lib_size")
res
res$plots$Hill_0
res$correlations
}

}
\seealso{
\code{\link[=estim_diff_pq]{estim_diff_pq()}}, \code{\link[=estim_cor_lpq]{estim_cor_lpq()}}
}
