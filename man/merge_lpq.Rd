% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge_lpq.R
\name{merge_lpq}
\alias{merge_lpq}
\title{Merge a list_phyloseq into a single phyloseq object}
\usage{
merge_lpq(
  x,
  match_by = c("refseq", "names"),
  tax_priority = 1L,
  verbose = TRUE
)
}
\arguments{
\item{x}{(list_phyloseq, required) A \link{list_phyloseq} object.}

\item{match_by}{(character, default \code{"refseq"}) How to match taxa
across phyloseq objects. One of:
\itemize{
\item \code{"refseq"}: match by DNA sequence in the \code{refseq} slot
(recommended). All phyloseq objects must have a \code{refseq} slot.
\item \code{"names"}: match by taxa names. Use only when taxa names are
consistent across objects (e.g., same pipeline, same database).
}}

\item{tax_priority}{(character or integer, default 1L) Which
phyloseq object's taxonomy to use when taxa are matched. Either
a name from the list or an integer index. When a taxon appears
in multiple objects, the taxonomy from the priority object is
used; if absent there, the first available taxonomy is used.}

\item{verbose}{(logical, default TRUE) Print information about the
merge.}
}
\value{
A phyloseq object with:
\describe{
\item{\code{otu_table}}{One column per original phyloseq object
(summed across its samples), one row per unique taxon.}
\item{\code{sample_data}}{One row per original phyloseq, with a
column \code{source_name} containing the list_phyloseq names.}
\item{\code{tax_table}}{Taxonomy from the priority object (or first
available).}
\item{\code{refseq}}{Present when \code{match_by = "refseq"}.}
}
}
\description{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange"
  alt="lifecycle-experimental"></a>
}}


Merges all phyloseq objects from a \link{list_phyloseq} into a single
phyloseq object where each original phyloseq becomes one sample.
Abundances are summed across samples within each phyloseq object.

Taxa are matched across phyloseq objects either by reference
sequences (\code{refseq} slot, default) or by taxa names. Matching by
refseq is preferred because taxa names are often inconsistent
across independently built phyloseq objects (e.g., ASV_1 in one
object is not the same taxon as ASV_1 in another).
}
\examples{
lpq <- list_phyloseq(list(
  fungi = data_fungi_mini,
  fungi2 = data_fungi_mini
))

# Merge by refseq (default)
merged <- merge_lpq(lpq)
merged

# Merge by taxa names
merged_names <- merge_lpq(lpq, match_by = "names")
}
\seealso{
\link{list_phyloseq}, \code{\link[=simple_venn_pq]{simple_venn_pq()}}
}
\author{
Adrien Taudière
}
