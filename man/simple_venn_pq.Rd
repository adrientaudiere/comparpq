% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simple_venn_pq.R
\name{simple_venn_pq}
\alias{simple_venn_pq}
\title{Venn diagram of shared taxa across sample groups}
\usage{
simple_venn_pq(
  physeq,
  fact,
  min_nb_seq = 0,
  taxonomic_rank = c("Class", "Order", "Family", "Genus", "Species"),
  na_remove = TRUE,
  count_type = c("rank", "taxa", "sequences"),
  add_nb_samples = TRUE,
  fill_alpha = 0.3,
  border_size = 0.8,
  text_size = 4,
  scale_text = FALSE,
  label_size = 4.5,
  colors = NULL,
  combine = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{physeq}{(phyloseq, required) A phyloseq object.}

\item{fact}{(character, required) Name of a variable in
\code{sample_data(physeq)} defining the groups (2-4 levels).}

\item{min_nb_seq}{(integer, default 0) Minimum total read count for
a taxon to be considered present in a group. A taxon must have
strictly more than \code{min_nb_seq} reads in a group to be included.}

\item{taxonomic_rank}{(character or NULL) Taxonomic rank(s) at
which to aggregate (via \code{\link[phyloseq:tax_glom]{phyloseq::tax_glom()}}) before computing
the Venn diagram. Defaults to all standard ranks (Kingdom through
Species). Use \code{NULL} to skip aggregation and work at ASV/OTU
level.}

\item{na_remove}{(logical, default TRUE) Remove samples with NA in
\code{fact} and, when aggregating, taxa with NA at \code{taxonomic_rank}.}

\item{count_type}{(character, default \code{"rank"}) What to count in
each Venn region. One of:
\itemize{
\item \code{"rank"}: number of unique taxonomic levels (e.g. number of
shared Classes). This is the default.
\item \code{"taxa"}: number of ASVs/OTUs assigned to the shared
taxonomic levels.
\item \code{"sequences"}: total number of reads for ASVs/OTUs assigned
to the shared taxonomic levels.
Ignored when \code{taxonomic_rank} is \code{NULL} (ASV-level), where
\code{"rank"} and \code{"taxa"} are equivalent.
}}

\item{add_nb_samples}{(logical, default TRUE) Append sample count
to group labels.}

\item{fill_alpha}{(numeric, default 0.3) Fill transparency for
shapes.}

\item{border_size}{(numeric, default 0.8) Border line width.}

\item{text_size}{(numeric, default 4) Base size of count labels
inside regions.}

\item{scale_text}{(logical, default FALSE) If \code{TRUE}, scale the
size of count labels proportionally to the count value. The
\code{text_size} parameter then acts as the base (minimum) size.}

\item{label_size}{(numeric, default 4.5) Size of group name labels.}

\item{colors}{(character or NULL) Vector of colors, one per group.
Defaults to a 4-color qualitative palette.}

\item{combine}{(logical, default TRUE) When \code{taxonomic_rank} has
length > 1, combine plots into a single patchwork figure. Set to
\code{FALSE} to return a named list of individual ggplot objects.
Requires the patchwork package.}

\item{verbose}{(logical, default TRUE) Print a message when no taxa
meet the criteria.}
}
\value{
A ggplot2 object (single rank), a patchwork object (multiple
ranks with \code{combine = TRUE}), or a named list of ggplot2 objects
(multiple ranks with \code{combine = FALSE}).
}
\description{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange"
  alt="lifecycle-experimental"></a>
}}


Draws a Venn diagram showing shared and unique taxa (or higher-rank
groups) across 2 to 4 levels of a sample variable, using only
ggplot2 (no external Venn diagram package needed).

When \code{taxonomic_rank} is a character vector of length > 1 (the
default), all ranks are displayed in a single combined figure using
the patchwork package (must be installed). Set \code{combine = FALSE} to
get a named list of individual plots instead.

For 2 and 3 groups, circles are used. For 4 groups, ellipses are
used to ensure all intersection regions are representable.
}
\examples{
# Default: all ranks combined in one figure
simple_venn_pq(data_fungi_mini, "Height")

# At genus level only
simple_venn_pq(data_fungi_mini, "Height", taxonomic_rank = "Genus")

# Multiple ranks as a list
plots <- simple_venn_pq(
  data_fungi_mini, "Height",
  taxonomic_rank = c("Family", "Genus"),
  combine = FALSE
)
plots[["Family"]]

# Count ASVs instead of rank levels
simple_venn_pq(data_fungi_mini, "Height",
  taxonomic_rank = "Genus", count_type = "taxa"
)

# Scale text by count value
simple_venn_pq(data_fungi_mini, "Height",
  taxonomic_rank = "Genus", scale_text = TRUE
)
}
\author{
Adrien Taudiere
}
