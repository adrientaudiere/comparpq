% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_lpq.R
\name{multipatt_lpq}
\alias{multipatt_lpq}
\title{Indicator species analysis on each phyloseq object in a list_phyloseq}
\usage{
multipatt_lpq(
  x,
  fact,
  p_adjust_method = "BH",
  pval = 0.05,
  control = permute::how(nperm = 999),
  verbose = TRUE,
  ...
)
}
\arguments{
\item{x}{(required) A list_phyloseq object.}

\item{fact}{(character, required) The name of a column in \code{sample_data}
to use as the grouping factor. Must be present in \strong{all} phyloseq
objects.}

\item{p_adjust_method}{(character, default "BH") The p-value adjustment
method. See \code{\link[stats:p.adjust]{stats::p.adjust()}} for available methods.}

\item{pval}{(numeric, default 0.05) The significance threshold for
adjusted p-values.}

\item{control}{(list, default \code{permute::how(nperm = 999)}) Permutation
control settings for the permutation test.}

\item{verbose}{(logical, default TRUE) If TRUE, print progress messages.}

\item{...}{Additional arguments passed to \code{\link[indicspecies:multipatt]{indicspecies::multipatt()}}.}
}
\value{
A tibble with the combined significant indicator taxa from all
phyloseq objects. Contains columns from \code{multipatt()$sign} output
plus \code{taxon} (taxon name), \code{p.adj} (adjusted p-value), and \code{name}
(identifying the source phyloseq object). Only taxa with
\code{p.adj < pval} are included.
}
\description{
Performs indicator species analysis using \code{\link[indicspecies:multipatt]{indicspecies::multipatt()}} on
each phyloseq object in a list_phyloseq and returns a combined result
table of significant indicator taxa.
}
\details{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental"></a>
}}


This function requires that the list_phyloseq type is NOT
\code{SEPARATE_ANALYSIS}, as the factor must be common across all phyloseq
objects.

Unlike \code{\link[MiscMetabar:multipatt_pq]{MiscMetabar::multipatt_pq()}} which returns a plot, this function
returns the underlying data as a tibble, making it easier to compare
results across phyloseq objects.

When no common taxa exist across the phyloseq objects, taxa names are
suffixed with the phyloseq object name to make them distinguishable.
}
\examples{
\dontrun{
lpq <- list_phyloseq(
  list(
    fungi = data_fungi,
    fungi_clust = postcluster_pq(data_fungi)
  ),
  same_bioinfo_pipeline = FALSE
)

results <- multipatt_lpq(lpq, fact = "Height")
results
}

}
\seealso{
\code{\link[indicspecies:multipatt]{indicspecies::multipatt()}}, \code{\link[MiscMetabar:multipatt_pq]{MiscMetabar::multipatt_pq()}},
\code{\link[=ancombc_lpq]{ancombc_lpq()}}, \code{\link[=aldex_lpq]{aldex_lpq()}}
}
