% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_primers_pq.R
\name{find_primers_pq}
\alias{find_primers_pq}
\title{Find taxa whose reference sequences match primer sequences}
\usage{
find_primers_pq(physeq, primers, verbose = TRUE)
}
\arguments{
\item{physeq}{(required) A \link[phyloseq:phyloseq]{phyloseq::phyloseq} object with a populated
\verb{@refseq} slot.}

\item{primers}{(required) A named character vector of primer sequences.
IUPAC ambiguity codes (M, R, Y, S, W, K, B, D, H, V, N) are supported.}

\item{verbose}{(logical, default \code{TRUE}) If \code{TRUE}, print a summary message.}
}
\value{
A \code{data.frame} (or \code{NULL} if no matches) with columns:
\describe{
\item{\code{taxon}}{Character. Taxa name as in \code{taxa_names(physeq)}.}
\item{\code{matched_primers}}{Character. Comma-separated names of matching
primers.}
\item{\code{n_reads}}{Numeric. Total read count across all samples
(\code{phyloseq::taxa_sums(physeq)}).}
}
}
\description{
Searches every sequence in \verb{@refseq} for occurrences of the supplied
primers (forward \strong{and} reverse complement) using IUPAC-aware matching via
\code{\link[Biostrings:vcountPattern]{Biostrings::vcountPattern()}}. Returns a data frame of taxa that match at
least one primer, which can be passed directly to
\code{tidypq::filter_taxa_pq()} to prune them from the phyloseq object.
}
\details{
Find taxa whose reference sequences match primer sequences.\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange"
  alt="lifecycle-experimental"></a>
}}
}
\examples{
primers <- c(
  mcrA_fwd = "GGTGGTGTMGGDTTCACMCARTA",
  mcrA_rev = "CGTTCATBGCGTAGTTVGGRTAGT"
)
bad <- find_primers_pq(data_fungi_mini, primers)
bad

# Prune contaminated taxa (requires tidypq):
# if (!is.null(bad)) {
#   tidypq::filter_taxa_pq(
#     data_fungi_mini,
#     !taxa_names(data_fungi_mini) \%in\% bad$taxon,
#     clean_phyloseq_object = TRUE
#   )
# }
}
\author{
Adrien Taudière
}
