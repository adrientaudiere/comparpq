% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estim_pq.R
\name{estim_diff_pq}
\alias{estim_diff_pq}
\title{Estimation statistics for categorical comparisons on a phyloseq object}
\usage{
estim_diff_pq(
  physeq,
  fact,
  hill_scales = c(0, 1, 2),
  custom_fn = NULL,
  effect_type = "cohens_d",
  idx = NULL,
  ci = 95,
  resamples = 5000,
  na_remove = TRUE,
  ...
)
}
\arguments{
\item{physeq}{(phyloseq, required) A phyloseq object.}

\item{fact}{(character, required) The name of a categorical column in
\code{sample_data} to use as the grouping factor.}

\item{hill_scales}{(numeric vector, default \code{c(0, 1, 2)}) The q values
for Hill number computation: 0 = richness, 1 = Shannon exponential,
2 = inverse Simpson.}

\item{custom_fn}{(function, default NULL) An optional custom diversity
function. Must take a phyloseq object and return a named numeric
vector (names = sample names) or a data.frame with one row per
sample. If provided, \code{hill_scales} is ignored.}

\item{effect_type}{(character, default \code{"mean_diff"}) The type of
effect size to compute. One of: \code{"mean_diff"}, \code{"median_diff"},
\code{"cohens_d"}, \code{"hedges_g"}, \code{"cliffs_delta"}.}

\item{idx}{(list or character vector, default NULL) The group ordering
for comparisons. If NULL, uses factor levels with first level as
control. For 2 groups: \code{c("Control", "Treatment")}. For 3+ groups:
\code{list(c("Ctrl", "T1", "T2"))}.}

\item{ci}{(numeric, default 95) Confidence interval level (0-100).}

\item{resamples}{(integer, default 5000) Number of bootstrap
resamples.}

\item{na_remove}{(logical, default TRUE) If TRUE, samples with NA in
\code{fact} are removed before analysis.}

\item{...}{Additional arguments passed to dabestr plotting functions.}
}
\value{
A list of class \code{"estim_diff_pq_result"} with components:
\describe{
\item{data}{The diversity data.frame used for analysis}
\item{dabest_objects}{A named list of dabestr objects (one per
metric)}
\item{plots}{A named list of dabestr plots (one per metric)}
\item{summary}{A tibble summarizing all effect sizes and CIs with
columns: \code{metric}, \code{comparison}, \code{effect_size}, \code{ci_lower},
\code{ci_upper}, \code{pvalue_permtest}, \code{pvalue_welch},
\code{pvalue_mann_whitney}}
\item{effect_type}{The effect size type used}
}
}
\description{
Computes diversity metrics (Hill numbers by default) per sample and
compares them across groups defined by a categorical variable using
estimation statistics (effect sizes + bootstrap confidence intervals)
via the \pkg{dabestr} package.

This approach replaces traditional p-value-based hypothesis testing
with Gardner-Altman or Cumming estimation plots, following the
estimation statistics framework (Ho et al. 2019).
}
\details{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange"
alt="lifecycle-experimental"></a>
}}


The function uses the \pkg{dabestr} package to produce estimation
plots. For two groups, Gardner-Altman plots are produced
(\code{float_contrast = TRUE}). For three or more groups, Cumming plots are
used (\code{float_contrast = FALSE}).
}
\examples{
library(phyloseq)
data("data_fungi", package = "MiscMetabar")

pq <- subset_samples(data_fungi, !is.na(Height))
pq <- clean_pq(pq)

res <- estim_diff_pq(pq, fact = "Height")
res
res$plots$Hill_0
res$summary

}
\references{
Ho, J., Tumkaya, T., Aryal, S., Choi, H., & Claridge-Chang, A.
(2019). Moving beyond P values: data analysis with estimation
graphics. \emph{Nature Methods}, 16(7), 565-566.
}
\seealso{
\code{\link[=estim_cor_pq]{estim_cor_pq()}}, \code{\link[=estim_diff_lpq]{estim_diff_lpq()}}, \code{\link[=adonis_lpq]{adonis_lpq()}}
}
