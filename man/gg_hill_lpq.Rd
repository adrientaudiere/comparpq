% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gg_hill_lpq.R
\name{gg_hill_lpq}
\alias{gg_hill_lpq}
\title{Scatter plots of Hill diversity across pairs of phyloseq objects}
\usage{
gg_hill_lpq(
  x,
  q = c(0, 1, 2),
  pairs = NULL,
  add_1to1 = TRUE,
  add_smooth = TRUE,
  cor_method = "pearson",
  point_color = "black",
  point_alpha = 0.6,
  point_size = 2,
  smooth_color = "steelblue",
  verbose = TRUE
)
}
\arguments{
\item{x}{(required) A \link{list_phyloseq} object.}

\item{q}{(numeric vector, default \code{c(0, 1, 2)}) Hill number
orders to compute: 0 = richness, 1 = Shannon exponential, 2 = inverse
Simpson.}

\item{pairs}{(list of integer pairs or NULL, default NULL) Which pairs of
phyloseq objects to compare. Each element must be a length-2 integer
vector of indices. If NULL, all pairwise combinations are used.}

\item{add_1to1}{(logical, default TRUE) If TRUE, adds a dashed 1:1
identity line to each panel.}

\item{add_smooth}{(logical, default TRUE) If TRUE, adds a linear
regression line with confidence interval via \code{\link[ggplot2:geom_smooth]{ggplot2::geom_smooth()}}.}

\item{cor_method}{(character, default \code{"pearson"}) Correlation method for
the annotation label. One of \code{"pearson"}, \code{"spearman"}, \code{"kendall"}.}

\item{point_color}{(character, default \code{"black"}) Color for scatter
points.}

\item{point_alpha}{(numeric, default 0.6) Transparency for scatter points.}

\item{point_size}{(numeric, default 2) Size for scatter points.}

\item{smooth_color}{(character, default \code{"steelblue"}) Color for the
regression line and confidence ribbon.}

\item{verbose}{(logical, default TRUE) If TRUE, print a message when
common samples are used instead of all samples.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object with panels arranged in a grid:
rows = pairs of phyloseq objects, columns = Hill number orders.
}
\description{
For each pair of phyloseq objects in a \link{list_phyloseq}, computes Hill
numbers (richness, Shannon exponential, inverse Simpson) per sample and
draws scatter plots comparing the diversity of the \strong{same samples} across
objects. This is a visual diagnostic for REPRODUCIBILITY, ROBUSTNESS, and
REPLICABILITY comparisons.

Each point represents one sample. A 1:1 reference line and optional
regression line help assess how similar (or different) the diversity
estimates are between two pipelines, runs, or primers.
}
\details{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange"
alt="lifecycle-experimental"></a>
}}


The function works on \strong{common samples} across all phyloseq objects. When
the \link{list_phyloseq} has \code{same_samples = TRUE}, all samples are used. When
samples differ (e.g., NESTED_ROBUSTNESS), only the intersection is kept.

Comparison type context:
\describe{
\item{REPRODUCIBILITY}{High correlation expected for all Hill orders.}
\item{ROBUSTNESS}{Moderate to high correlation is desirable; deviations
indicate pipeline sensitivity.}
\item{REPLICABILITY}{Correlation may vary across Hill orders depending on
primer or technology differences.}
}
}
\examples{
lpq <- list_phyloseq(
  list(run1 = data_fungi, run2 = data_fungi_mini),
  same_bioinfo_pipeline = FALSE
)

gg_hill_lpq(lpq)
gg_hill_lpq(lpq, q = c(0, 1))
gg_hill_lpq(lpq, add_smooth = FALSE, add_1to1 = TRUE)

}
\seealso{
\code{\link[=estim_cor_pq]{estim_cor_pq()}}, \code{\link[=estim_diff_lpq]{estim_diff_lpq()}}
}
