% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_lpq.R
\name{aldex_lpq}
\alias{aldex_lpq}
\title{ALDEx2 analysis on each phyloseq object in a list_phyloseq}
\usage{
aldex_lpq(x, bifactor, modalities = NULL, gamma = 0.5, verbose = TRUE, ...)
}
\arguments{
\item{x}{(required) A list_phyloseq object.}

\item{bifactor}{(character, required) The name of a dichotomous column
in \code{sample_data} to use as the grouping factor. Must be present in
\strong{all} phyloseq objects.}

\item{modalities}{(character vector, default NULL) Two modalities of
\code{bifactor} to compare. If NULL, uses the two levels present in the
data.}

\item{gamma}{(numeric, default 0.5) The gamma parameter for ALDEx2.}

\item{verbose}{(logical, default TRUE) If TRUE, print progress messages.}

\item{...}{Additional arguments passed to \code{\link[MiscMetabar:aldex_pq]{MiscMetabar::aldex_pq()}}.}
}
\value{
A tibble with the combined ALDEx2 results from all phyloseq
objects. Each row corresponds to one taxon in one phyloseq object,
with columns from ALDEx2 output plus \code{taxon} (from rownames) and
\code{name} (identifying the source phyloseq object).
}
\description{
Performs ALDEx2 differential abundance analysis using
\code{\link[MiscMetabar:aldex_pq]{MiscMetabar::aldex_pq()}} on each phyloseq object in a list_phyloseq
and returns a combined result table.
}
\details{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental"></a>
}}


This function requires that the list_phyloseq type is NOT
\code{SEPARATE_ANALYSIS}, as the bifactor must be common across all phyloseq
objects.

When no common taxa exist across the phyloseq objects, taxa names are
suffixed with the phyloseq object name to make them distinguishable.
}
\examples{
\dontrun{
lpq <- list_phyloseq(
  list(
    fungi = data_fungi,
    fungi_clust = postcluster_pq(data_fungi)
  ),
  same_bioinfo_pipeline = FALSE
)

results <- aldex_lpq(lpq,
  bifactor = "Height",
  modalities = c("Low", "High")
)
results

ALDEx2::aldex.plot(filter(results, name == "fungi"), type = "volcano")
ALDEx2::aldex.plot(filter(results, name == "fungi_clust"), type = "volcano")

ggplot(results, aes(y = taxon, x = effect, col = wi.eBH)) +
  geom_point()
}

}
\seealso{
\code{\link[MiscMetabar:aldex_pq]{MiscMetabar::aldex_pq()}}, \code{\link[=ancombc_lpq]{ancombc_lpq()}}, \code{\link[=multipatt_lpq]{multipatt_lpq()}}
}
