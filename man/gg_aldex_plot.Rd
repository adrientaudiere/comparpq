% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggaldex.R
\name{gg_aldex_plot}
\alias{gg_aldex_plot}
\title{ggplot2 version of ALDEx2 diagnostic plots}
\usage{
gg_aldex_plot(
  x,
  type = c("MW", "MA", "volcano", "volcano.var"),
  test = c("welch", "wilcox", "effect", "both"),
  cutoff.pval = 0.05,
  cutoff.effect = 1,
  rare = 0,
  all.col = "grey30",
  called.col = "red",
  rare.col = "black",
  point.size = 1.5,
  point.alpha = 0.4
)
}
\arguments{
\item{x}{(data.frame or tibble, required) ALDEx2 results, typically
from \code{\link[ALDEx2:aldex]{ALDEx2::aldex()}} or \code{\link[=aldex_lpq]{aldex_lpq()}}. Must contain columns
\code{diff.btw}, \code{diff.win}, \code{rab.all}, and \code{effect}. When \code{test} is
\code{"welch"}, column \code{we.eBH} is required; when \code{"wilcox"}, column
\code{wi.eBH} is required.}

\item{type}{(character, default "MW") Plot type. One of \code{"MW"}
(dispersion vs difference), \code{"MA"} (abundance vs difference),
\code{"volcano"} (difference vs -log10 q-value), or \code{"volcano.var"}
(dispersion vs -log10 q-value).}

\item{test}{(character, default "welch") Statistical test used for
significance calling. One of \code{"welch"}, \code{"wilcox"}, \code{"effect"},
or \code{"both"} (effect + welch).}

\item{cutoff.pval}{(numeric, default 0.05) q-value threshold for
significance.}

\item{cutoff.effect}{(numeric, default 1) Effect size threshold
(must be >= 0.5 when \code{test} is \code{"effect"} or \code{"both"}).}

\item{rare}{(numeric, default 0) Abundance threshold below which
taxa are marked as rare (only used for MW and MA plots).}

\item{all.col}{(character, default "grey30") Color for non-significant
points.}

\item{called.col}{(character, default "red") Color for significant
points.}

\item{rare.col}{(character, default "black") Color for rare taxa
points.}

\item{point.size}{(numeric, default 1.5) Size of points.}

\item{point.alpha}{(numeric, default 0.4) Alpha transparency of
non-significant points.}
}
\value{
A ggplot2 object. If \code{x} contains a \code{name} column (e.g.,
from \code{\link[=aldex_lpq]{aldex_lpq()}}), the plot is faceted by \code{name}.
}
\description{
Creates ggplot2 versions of the four ALDEx2 diagnostic plot types:
MW (Bland-Altman style), MA, volcano, and volcano.var. Supports
faceting by a \code{name} column, making it suitable for output from
\code{\link[=aldex_lpq]{aldex_lpq()}}.
}
\details{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental"></a>
}}


This function reimplements \code{\link[ALDEx2:aldex.plot]{ALDEx2::aldex.plot()}} using ggplot2,
providing a more customizable and composable output. The four plot
types correspond to the original ALDEx2 types:

\describe{
\item{MW}{Bland-Altman style: within-condition dispersion (x) vs
between-condition difference (y), with +/-1 effect size lines.}
\item{MA}{MA plot: median log2 relative abundance (x) vs median
log2 difference (y).}
\item{volcano}{Volcano plot: median log2 difference (x) vs
-log10 q-value (y).}
\item{volcano.var}{Variance volcano: median log2 dispersion (x)
vs -log10 q-value (y).}
}
}
\examples{

data_fungi_high <- multiply_counts_pq(data_fungi, "Height", "High",
  4,
  prop_taxa = 0.1, seed = 42
)

aldex_pq(data_fungi_high,
  bifactor = "Height",
  modalities = c("Low", "High")
) |>
  gg_aldex_plot()

lpq <- list_phyloseq(
  list(
    fungi = data_fungi,
    fungi_height = data_fungi_high
  ),
  same_bioinfo_pipeline = FALSE
)
# From aldex_lpq (faceted by name)
lpq_res <- aldex_lpq(lpq,
  bifactor = "Height",
  modalities = c("Low", "High")
)
gg_aldex_plot(lpq_res, type = "MA")
gg_aldex_plot(lpq_res, type = "MW", test = "wilcox")

gg_aldex_plot(lpq_res, type = "volcano")
gg_aldex_plot(lpq_res, type = "volcano.var")


gingival_pq <-
  MicrobiomeBenchmarkData::getBenchmarkData("HMP_2012_16S_gingival_V35_subset",
    dryrun = FALSE
  )[[1]] |>
  mia::convertToPhyloseq()

aldex_res <- aldex_pq(gingival_pq,
  bifactor = "body_subsite",
  modalities = c("supragingival_plaque", "subgingival_plaque")
)

gg_aldex_plot(aldex_res, type = "volcano")

}
\seealso{
\code{\link[=aldex_lpq]{aldex_lpq()}}, \code{\link[ALDEx2:aldex.plot]{ALDEx2::aldex.plot()}}
}
